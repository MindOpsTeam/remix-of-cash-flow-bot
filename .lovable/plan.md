

# Regenerar types.ts

Executar uma migration no-op (`SELECT 1;`) para forçar a regeneração automática do arquivo `src/integrations/supabase/types.ts`, refletindo os triggers, constraint e realtime adicionados recentemente.

Nenhum outro arquivo será alterado.

